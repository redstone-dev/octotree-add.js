function init(configType, colour, bgColour){
  var ghConfig = {
    _config: configType,
    _colour: colour,
    _bg: bgColour,
    _defaults: ["_config: configure", 
    "_colour: white", 
     "_bg: black" ] 
  };
}
